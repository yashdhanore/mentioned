# GitHub Issue Manifest: Release 1 iOS-First Published Proof

Source PRD: `docs/prds/2026-05-28-release-1-ios-published-proof.md`
Created: 2026-05-28
Status: GitHub issues created for approved substantive work. Manifest issue 11 skipped as completed by creation run.

## Pre-Creation Notes

- Duplicate searches were run before creation for native capture, iOS share extension, correction controls, release readiness, and source detail polish; no open matches were returned.
- The repository only had standard GitHub labels available, so feature/enhancement work was created with `enhancement` and release/checklist work was created with `documentation`.

## Created GitHub Issues

| Manifest # | Issue | Label | URL |
| --- | --- | --- | --- |
| 1 | Add iOS share extension for native source capture | `enhancement` | https://github.com/yashdhanore/mentioned/issues/13 |
| 2 | Route shared iOS URLs into the existing job creation flow | `enhancement` | https://github.com/yashdhanore/mentioned/issues/14 |
| 3 | Preserve a shared source through sign-in | `enhancement` | https://github.com/yashdhanore/mentioned/issues/15 |
| 4 | Make saved source detail extracted-books-first | `enhancement` | https://github.com/yashdhanore/mentioned/issues/16 |
| 5 | Add mobile correction controls for extracted books | `enhancement` | https://github.com/yashdhanore/mentioned/issues/17 |
| 6 | Persist lightweight "wrong result" feedback for extracted books | `enhancement` | https://github.com/yashdhanore/mentioned/issues/18 |
| 7 | Polish Release 1 loading, empty, no-books, and failure states | `enhancement` | https://github.com/yashdhanore/mentioned/issues/19 |
| 8 | Prepare iOS EAS/TestFlight release configuration | `documentation` | https://github.com/yashdhanore/mentioned/issues/20 |
| 9 | Add Release 1 smoke-test checklist for native capture and extraction | `documentation` | https://github.com/yashdhanore/mentioned/issues/21 |
| 10 | Verify backend readiness and guardrails for the published proof | `documentation` | https://github.com/yashdhanore/mentioned/issues/22 |

Manifest issue 11 was not created because duplicate search and issue creation were completed by this run.

## Proposed Issues

### 1. Add iOS share extension for native source capture

Type label suggestion: `type:feature`

## Summary

Add an iOS share extension so Mentioned appears in the iOS share sheet for compatible shared URLs/text. This is the primary Release 1 capture path and should let a user share an Instagram/Reel source to Mentioned without copying and pasting manually.

## Acceptance Criteria

- [ ] Mentioned appears as a share target from the iOS share sheet for shared URL/text payloads.
- [ ] The extension can receive shared text containing a supported source URL.
- [ ] Unsupported or empty share payloads are handled without crashing.
- [ ] The implementation is compatible with the Expo/EAS iOS build path used by the mobile app.
- [ ] Android behavior is not changed in this issue.

## Technical Notes

- Likely files: `mobile/app.json`, native iOS extension files/config generated by Expo prebuild or a config plugin, `mobile/eas.json`.
- Patterns to follow: keep the share extension as a native capture entry point; the backend remains responsible for extraction.
- Dependencies: none.

## Validation

```bash
cd mobile
npm run typecheck
```

Manual validation: build/run an iOS development build or simulator build and verify Mentioned appears in the system share sheet from an app that can share text/URLs.

---

### 2. Route shared iOS URLs into the existing job creation flow

Type label suggestion: `type:feature`

## Summary

Connect native-captured URLs to the existing mobile capture flow so a shared source creates a backend job, appears as a saved source, shows processing state, and opens the saved source detail.

## Acceptance Criteria

- [ ] Shared text is parsed for a supported source URL.
- [ ] A valid shared URL calls the same job creation API path as paste-link capture.
- [ ] The newly created capture appears at the top of the saved source home.
- [ ] The app selects or opens the new processing capture after successful native capture.
- [ ] Invalid shared content shows a calm, user-facing error.
- [ ] Paste-link capture continues to work.

## Technical Notes

- Likely files: `mobile/App.tsx`, `mobile/src/features/captures/use-captures.ts`, `mobile/src/api.ts`, `mobile/src/utils/source-url.ts`.
- Patterns to follow: reuse `createJob`, `captureFromJobCreated`, `refreshCaptureById`, and existing URL validation helpers where possible.
- Dependencies: Issue 1.

## Validation

```bash
cd mobile
npm run typecheck
```

Manual validation: share a supported Instagram/Reel URL into Mentioned and confirm it follows the same visible flow as a pasted URL.

---

### 3. Preserve a shared source through sign-in

Type label suggestion: `type:feature`

## Summary

Handle native capture when the user is signed out so a shared source is not silently lost. The app should guide the user through sign-in and then resume or confirm submission of the pending shared source.

## Acceptance Criteria

- [ ] When a signed-out user shares a supported source to Mentioned, the app preserves the pending source locally.
- [ ] The signed-out screen explains that sign-in is needed before saving the shared source.
- [ ] After successful sign-in, the app can submit or prompt the user to submit the pending source.
- [ ] The pending source is cleared after successful submission or explicit cancellation.
- [ ] Pending source storage does not leak secrets or auth tokens.

## Technical Notes

- Likely files: `mobile/App.tsx`, `mobile/src/supabase.ts`, `mobile/src/features/captures/use-captures.ts`.
- Patterns to follow: existing Supabase auth session handling and mobile state management.
- Dependencies: Issue 2.

## Validation

```bash
cd mobile
npm run typecheck
```

Manual validation: sign out, share a supported source to Mentioned, sign in, and confirm the source can still be saved.

---

### 4. Make saved source detail extracted-books-first

Type label suggestion: `type:enhancement`

## Summary

Rework the saved source detail hierarchy for Release 1 so extracted books are the first meaningful content. Source preview, source context, and "Open on Instagram" remain available but secondary.

## Acceptance Criteria

- [ ] Ready source detail screens show extracted books before the large source preview/context block.
- [ ] Processing, failed, and no-books states remain clear and reachable.
- [ ] The original source remains reopenable from the detail screen.
- [ ] The screen does not expose confidence scores, provider names, artifact paths, or backend internals.
- [ ] The layout remains readable on small and large supported iPhone widths.

## Technical Notes

- Likely files: `mobile/src/screens/reel-detail-screen.tsx`, `mobile/src/components/books.tsx`, `mobile/src/styles.ts`.
- Patterns to follow: existing design system in `DESIGN.md` and current mobile component structure.
- Dependencies: none.

## Validation

```bash
cd mobile
npm run typecheck
```

Manual validation: inspect ready, processing, failed, and no-books source detail states.

---

### 5. Add mobile correction controls for extracted books

Type label suggestion: `type:feature`

## Summary

Let users perform basic correction on extracted books from a saved source. Release 1 should support removing wrong books and making simple title/author edits without introducing a full catalog management workflow.

## Acceptance Criteria

- [ ] A user can remove an incorrect extracted book from a saved source.
- [ ] A user can edit a book title.
- [ ] A user can edit a book author.
- [ ] Correction actions update the UI without requiring a full app restart.
- [ ] Correction errors are shown calmly and do not strand the user.
- [ ] Advanced metadata editing remains out of scope.

## Technical Notes

- Likely files: `mobile/src/api.ts`, `mobile/src/components/books.tsx`, `mobile/src/components/sheets.tsx`, `mobile/src/features/captures/use-captures.ts`.
- Existing backend endpoints: `PATCH /v1/mentions/{mention_id}` and `DELETE /v1/mentions/{mention_id}`.
- Dependencies: Issue 4.

## Validation

```bash
cd mobile
npm run typecheck
python -m pytest tests/mentions
```

Manual validation: correct and remove books from a completed saved source, then refresh and confirm changes persist.

---

### 6. Persist lightweight "wrong result" feedback for extracted books

Type label suggestion: `type:feature`

## Summary

Add a lightweight feedback path so users can mark an extracted book/result as wrong. This gives Release 1 a basic trust and quality signal without requiring full analytics or model feedback infrastructure.

## Acceptance Criteria

- [ ] A user can mark an extracted book as wrong from the mobile UI.
- [ ] Wrong-result feedback is persisted server-side and scoped to the authenticated user.
- [ ] The feedback path does not expose provider details or internal confidence values.
- [ ] The API rejects feedback for mentions the caller does not own.
- [ ] Existing mention update/delete behavior continues to work.

## Technical Notes

- Likely files: `src/mentions/models.py`, `src/mentions/schemas.py`, `src/mentions/router.py`, `src/mentions/service.py`, Alembic migration under `migrations/versions/`, mobile API/component files.
- Patterns to follow: existing mention ownership dependencies and migration style.
- Dependencies: Issue 5 can build the UI affordance, but backend persistence can be implemented independently.

## Validation

```bash
python -m pytest tests/mentions
cd mobile
npm run typecheck
```

Manual validation: mark a result wrong and confirm the state survives refresh.

---

### 7. Polish Release 1 loading, empty, no-books, and failure states

Type label suggestion: `type:enhancement`

## Summary

Bring the visible app states up to the trustworthy-small-app bar for Release 1. The app should feel intentional when loading saved sources, processing extraction, finding no books, failing extraction, or rejecting invalid shared content.

## Acceptance Criteria

- [ ] Saved source home loading and empty states match the current product promise.
- [ ] Processing copy clearly says the app is finding books.
- [ ] No-books state explains the outcome without implying data loss.
- [ ] Failed state includes retry and source-opening recovery where appropriate.
- [ ] Invalid shared content has a clear user-facing message.
- [ ] The app does not show disabled future feature screens.

## Technical Notes

- Likely files: `mobile/src/screens/home-screen.tsx`, `mobile/src/screens/reel-detail-screen.tsx`, `mobile/src/components/books.tsx`, `mobile/src/components/ui.tsx`, `mobile/src/styles.ts`.
- Patterns to follow: design source of truth in `DESIGN.md`; avoid raw backend/provider language.
- Dependencies: Issues 2 and 4 for full native-capture and detail coverage.

## Validation

```bash
cd mobile
npm run typecheck
```

Manual validation: inspect home, processing, ready, no-books, failed, invalid-share, and signed-out states.

---

### 8. Prepare iOS EAS/TestFlight release configuration

Type label suggestion: `type:chore`

## Summary

Prepare the mobile app for Apple-distributed installation through EAS/TestFlight/App Store Connect. Release 1 is not complete if it only runs locally.

## Acceptance Criteria

- [ ] iOS bundle identifier, scheme, app metadata, icon, splash screen, and build profiles are verified for Release 1.
- [ ] Production mobile config requires non-local HTTPS API and Supabase values.
- [ ] Privacy policy URL is configured for production builds.
- [ ] EAS iOS build settings are documented and usable.
- [ ] App Store reviewer notes are drafted for the native capture core loop.
- [ ] No Android release work is required in this issue.

## Technical Notes

- Likely files: `mobile/app.json`, `mobile/eas.json`, `mobile/README.md`, release docs if added.
- Patterns to follow: existing production runtime config checks in `mobile/src/api.ts`.
- Dependencies: Issue 1 for final native capture build behavior.

## Validation

```bash
cd mobile
npm run typecheck
```

Manual validation: run an iOS EAS development or preview build once Apple credentials are available.

---

### 9. Add Release 1 smoke-test checklist for native capture and extraction

Type label suggestion: `type:chore`

## Summary

Create a release smoke-test checklist that verifies the native capture core loop, existing paste fallback, auth behavior, extraction completion, correction controls, and production configuration before TestFlight/App Store submission.

## Acceptance Criteria

- [ ] Checklist covers signed-in native capture happy path.
- [ ] Checklist covers signed-out native capture behavior.
- [ ] Checklist covers paste-link fallback.
- [ ] Checklist covers processing, ready, no-books, failed, retry, remove, edit, and mark-wrong states.
- [ ] Checklist includes backend worker/API health and a real-source smoke test.
- [ ] Checklist includes app production config and privacy policy verification.

## Technical Notes

- Likely files: new release doc under `docs/` or `.agents/reports/`.
- Patterns to follow: existing smoke script references in `README.md` and Render/Supabase deploy docs.
- Dependencies: Issues 1, 2, 3, 5, 6, and 8 for complete checklist execution.

## Validation

```bash
python -m pytest
cd mobile
npm run typecheck
```

Manual validation: execute the checklist against a staging or production-like backend before store submission.

---

### 10. Verify backend readiness and guardrails for the published proof

Type label suggestion: `type:chore`

## Summary

Review and tighten backend readiness for a small public iOS proof. The goal is not major scale optimization; it is confirming migrations, auth, worker processing, quotas, logs, and failure diagnosis are good enough for early real users.

## Acceptance Criteria

- [ ] Production/staging migrations are accounted for before app submission.
- [ ] API and worker deployment configuration is documented and checked.
- [ ] Per-user active/daily job guardrails are verified for Release 1.
- [ ] Worker failure and retry behavior is understandable from logs.
- [ ] A real-source smoke test can create a job, process it, and return saved mentions.
- [ ] No provider cost redesign or queue architecture redesign is introduced.

## Technical Notes

- Likely files: `src/config.py`, `src/jobs/router.py`, `src/worker.py`, `README.md`, `docs/render-supabase-deploy.md`, `scripts/check_release_env.py`, `scripts/smoke_job_flow.py`.
- Patterns to follow: existing Render/Supabase deployment guidance and current job quota checks.
- Dependencies: none, but should be completed before App Store submission.

## Validation

```bash
python -m pytest
python scripts/check_release_env.py --env-file .env --worker-replicas 1
```

Manual validation: run `scripts/smoke_job_flow.py` against the intended backend with a real supported source and a valid test user token.

---

### 11. Run duplicate search and create approved GitHub issues

Type label suggestion: `type:chore`

## Summary

After the local manifest is approved, search existing open GitHub issues for duplicates, create the approved issues, and update this manifest with issue numbers and URLs.

## Acceptance Criteria

- [ ] Existing open GitHub issues are searched before creation.
- [ ] Duplicates are skipped or linked in the manifest.
- [ ] Approved issues are created with the agreed titles and bodies.
- [ ] This manifest is updated with issue numbers and URLs.
- [ ] No unapproved issues are created.

## Technical Notes

- Likely file: `.agents/stories/release-1-ios-published-proof.github-issues.md`.
- Patterns to follow: `.agents/commands/create-github-issues.md`.
- Dependencies: user approval to create issues.

## Validation

```bash
gh issue list --search "iOS share extension Mentioned native capture" --state open
gh issue list --search "Release 1 iOS published proof Mentioned" --state open
```
