PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# Used at build time (astro.config.mjs) as the canonical site origin for the sitemap and
# canonical/OG URLs. Defaults to https://mentioned-web.onrender.com; override for a preview
# deploy or custom domain.
PUBLIC_SITE_URL=
